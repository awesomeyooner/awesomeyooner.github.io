class Constants{

    static BOUNDING_WIDTH = 600;
    static BOUNDING_HEIGHT = 400;

    static CORNERS = [
        new Point(-this.BOUNDING_WIDTH / 2, this.BOUNDING_HEIGHT / 2),
        new Point(this.BOUNDING_WIDTH / 2, this.BOUNDING_HEIGHT / 2),
        new Point(this.BOUNDING_WIDTH / 2, -this.BOUNDING_HEIGHT / 2),
        new Point(-this.BOUNDING_WIDTH / 2, -this.BOUNDING_HEIGHT / 2)
    ];

    /*

    | 0  1 |
    | 2  3 |

    */
}